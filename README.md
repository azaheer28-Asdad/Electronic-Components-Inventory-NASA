Electronic-Component-Scanner-and-Cataloger

I don't like manual labor. I don't want to have to read the number off each label for each electronic component and log them on a spreadsheet; that sounds boring. But what doesn't sound boring is writing a python script using openCV and connecting to an ollama model for OCR or so and then import the results back to the python script so that it can log them into a spreadsheet. But what doesn't sound boring is writing a Python script using OpenCV and connecting to an ollama model for OCR, or something like that, and then importing the results back into the Python script so it can log them to a spreadsheet.
